# TODO.md — 다음에 할 일

> 현재 상태는 [HANDOFF.md](HANDOFF.md), 디자인 규칙은 [DESIGN.md](DESIGN.md) 참고.
> 완료한 항목은 `[ ]` → `[x]`로 바꾸고, `CLAUDE.md` 작업 로그에도 한 줄 남깁니다.
> 최종 업데이트: **2026-06-04 (프로젝트 관리 화면 — As-Is 갤러리)**

---

## 🔴 내일 최우선 1 — Vercel 배포본 보안 강화 (크롤링·직접접근 차단)

> **배경**: 현재 `https://atg-asset.vercel.app`에 배포된 상태. 프로젝트 관리 화면의 텍스트·이미지가 HTML 소스/직접 URL/구글 크롤링으로 노출될 수 있음. **오늘 배포 금지 — 아래 작업 완료 후 배포.**

- [x] **수준 1 — robots.txt + noindex** (즉시 적용)
  - 프로젝트 루트에 `robots.txt` 생성 (`User-agent: * / Disallow: /`)
  - `index.html` `<head>`에 `<meta name="robots" content="noindex, nofollow">` 추가
- [x] **수준 2 — 이미지 동적 삽입** (실질적 URL 노출 차단)
  - `images/proj-screens/` 이미지 14장의 `<img src>` 를 빈 문자열로 초기화
  - `enterProjectMode()` 실행 시점에 실제 경로를 동적으로 주입 → HTML 소스·크롤러에 이미지 URL 미노출
- [ ] 작업 완료 후 Vercel 배포 진행

---

## ⭐ 내일 이어서 (최우선 2) — 프로젝트 관리: "AI 에이전트 분석" 갤러리화

> **무엇**: 비밀 프로젝트 관리 화면(`#/`에서 사이드바 `v0.9.3` 클릭 → 비번 `78963`)의 **"AI 에이전트 분석 (우리은행 제안 참고)"** 섹션(`#pj-ai`)을, 방금 만든 **As-Is 갤러리와 똑같은 방식**(화면별 이미지 + 옆/아래 설명 카드)으로 확장한다.
>
> **참고**: As-Is 갤러리 구현(`#pj-asis`)을 그대로 본떠서 진행하면 됨. 재사용 가능한 CSS·구조 이미 존재.

- [ ] **원본 이미지 준비(자르기)** — 우리은행 AI에이전트 제안 3종은 `C:\Users\user\Pictures\Screenshots\`에 있음:
  - `2.3 AI 서비스 플랫폼.png` (≈9976×28239), `2.3.3 Multi Agent FW.png` (≈11359×14671), `2.4 AI 모델 영역(LLM Ops).png` (≈13532×17136)
  - ⚠️ **한 파일에 여러 화면이 세로로 쌓인 초대형 이미지** → As-Is처럼 단순 복사가 아니라 **화면(섹션) 단위로 crop** 해야 함. (PowerShell `System.Drawing`로 영역 잘라 저장 — 이전 세션에서 쓴 Crop 스크립트 방식)
  - 자른 이미지는 `images/proj-screens/`에 `ai-*.png`로 저장(예: `ai-platform-mcp.png`, `ai-multiagent-role.png`, `ai-llmops-catalog.png` 등).
- [ ] **설명문 작성** — 각 화면 설명은 이미 분석해 둔 **`AI_AGENT_제안분석.md`**(3계층·영역별 표) 내용을 옮겨 쓰면 됨. 우리 자산관리로 번역한 포인트도 함께.
- [ ] **사이드바 2차 트리 확장** — `#project-nav`의 "AI 에이전트 분석" 링크를 As-Is처럼 `.pj-subgroup`(펼침형)으로 바꾸고, ① 서비스 플랫폼 ② Multi Agent FW ③ LLM Ops 3분류 + 화면별 `.pj-sublink`(data-pj=`pjs-ai-...`) 추가.
- [ ] **본문 카드 추가** — `#pj-ai` 섹션에 `.pj-screen`(이미지 `.pj-shot` + 설명 `.pj-screen-desc`) 카드들을 화면별로 추가. **라이트박스·배지·반응형 스택은 기존 그대로 동작**(추가 CSS 불필요).
- [ ] 완료 후 Playwright 테스트(`tests/project-secret.spec.js`)에 AI 갤러리 케이스 1~2개 추가.

> 💡 핵심: As-Is 갤러리(`index.html`의 `#pjs-ps-*` 카드들 + `#project-nav`의 `.pj-subgroup`)를 **복붙·수정**하는 작업이라 빠르게 가능. 가장 손 많이 가는 건 "초대형 원본을 화면별로 잘라내기".

---

## 완료된 항목 ✅
- [x] 프로젝트 기본 뼈대 (index.html + css + js)
- [x] 샘플 자산 데이터 71건 (js/data.js)
- [x] Figma 시안 기반 사이드바·헤더·KPI 디자인 적용
- [x] 원장관리 — 자산 목록·검색·상세·이력
- [x] 대시보드 전면 개편 (시안 `기본화면_대시보드.png` 기준)
  - AI 인사이트 배너, KPI 6칸(게이지 포함), 노후 위험도 도넛
  - 교체 시기 타임라인, 한국 지도(지역 핀·팝업), 위험자산 TOP5
  - 최근 이슈, 계약/점검 일정, 노후도 분석 막대, AI 플로팅 버튼
- [x] 사이드바 리디자인 — NH 아이콘 로고, 새 메뉴 아이콘 11개, 토글 버튼(내부 배치), 활성 스타일 개선
- [x] 대시보드 메뉴 이중 아이콘 — 비활성(Icon_dashboard.svg 흰색) / 활성(Icon_dashboard2.svg 남색)
- [x] 헤더 탭(고정자산관리시스템 / 지역별 현황 / 현장 점검) + 리포트 생성 버튼
- [x] SPA URL 해시 라우팅 (#/dashboard, #/list, #/detail/{id}, #/soon, #/ai-agent, #/report)
- [x] 브라우저 뒤로/앞으로 버튼 지원
- [x] Playwright 자동 테스트 (스크린샷 + 기본 동작 6개 + AI Agent 4개)
- [x] GitHub 백업 (siche7-iu/asset)
- [x] 자산 등록 모달 (+ 자산 등록 버튼 → 9개 필드 → 목록에 즉시 추가)
- [x] 자산 상세 보강 (감가상각 게이지, 담당자 연락처 카드, 첨부파일 플레이스홀더)
- [x] 대시보드 진입 애니메이션 (요소 순차 fadeUp + KPI 카운터 + 게이지 성장, 재진입 시 매번 재생)
- [x] AI Agent 화면 (추천질문 → 자동분석 → 결과테이블 → 자산 상세 슬라이드)
- [x] AI Agent 진입 애니메이션 (요소 순차 등장 + 인사 메시지 pop-in)
- [x] AI Agent 분석 단계 로딩 스피너 (진행 중 단계에 파란 호 애니메이션)
- [x] AI Agent 채팅 이력 누적 + 레이아웃 고정·내부 스크롤
- [x] AI Agent 퍼지 매칭 (오타·띄어쓰기 허용, 3단계 매칭)
- [x] 보고서 화면 (#/report) — 유형 선택 + 본문 자동 생성(시나리오2 차량 보험 6섹션)
- [x] 스크롤 구조 개선 — `.content` 내부 스크롤, 헤더/사이드바 항상 고정
- [x] 알림 팝업 — 벨 아이콘 클릭 시 말풍선(해제·지우기·모두지우기·배지 연동·클릭 시 화면 이동)
- [x] 내 업무 현황 팝업 — 아바타 버튼 클릭 시 말풍선(알림 팝업과 상호 배타)
- [x] 지역별 관리 현황 — 도 전체 영역 hover 시 상세 말풍선 표시 + fade 전환
- [x] 지역별 관리 현황 — 말풍선 방향(dir-left/right, exp-above) 버그 수정
- [x] KPI 카드 hover 효과 (확대 + 이웃 카드 흐림)
- [x] 노후 위험도 도넛 · 타임라인 · 노후도 분석 막대 마우스오버 툴팁
- [x] AI INSIGHT 배너 + FAB 버튼 shimmer 호버 애니메이션
- [x] 로고 클릭 → 어느 화면에서든 대시보드 이동
- [x] FAB 버튼 아이콘 ai_insight.svg로 교체
- [x] Figma 원본(node 209:2622) MCP 분석 → FIGMA_SYNC_PLAN.md 작성, DESIGN.md 실측값 갱신
- [x] 시연 자막 파일 (시연_자막.txt) 생성
- [x] **알림 팝업 추가 다듬기** — 각 알림 클릭 시 해당 화면으로 이동, 읽음 처리 스타일.
- [x] **Figma 동기화 Phase 1** — 헤더 탭 "자산현황"→"지역별 현황", 타임라인 제목 "2차 시기 도래"→"교체 시기 도래" 텍스트 오류 수정.
- [x] **Supabase DB 연동** — `assets` / `asset_history` 테이블 생성, 샘플 데이터 71건 삽입. 앱 시작 시 DB 우선 로드·로컬 폴백. 자산 등록 모달 → DB 영구 저장. DB 연결 상태 토스트 표시.

---

## A. 바로 해볼 수 있는 것 (우선순위 높음)

- [ ] **Playwright 테스트 업데이트** — 대시보드 개편 내용(KPI 수치·지도·도넛 등)과 AI Agent·알림 팝업·보고서 화면 테스트 추가.
- [ ] **사이드바 나머지 메뉴 아이콘** — 이미지 파일이 제공되면 대시보드처럼 이중 이미지(비활성/활성) 방식으로 교체.
- [ ] **Supabase 자산 수정/삭제** — 자산 상세 화면에서 정보 수정·상태 변경을 DB에도 반영 (현재는 읽기+등록만 가능).
- [ ] **Supabase 실시간(Realtime)** — 다른 브라우저에서 자산 등록 시 현재 화면에도 즉시 반영 (선택 사항).

---

## B. Figma 동기화 (FIGMA_SYNC_PLAN.md 참고)

> Phase 1 완료. Phase 2~5는 지시 시 진행.

- [ ] **Phase 2** — CSS 수치 조정 (사이드바 폭 220→264px, gap 16→20px, KPI 카드 높이 132→160px 등)
- [ ] **Phase 3** — 대시보드 레이아웃 구조 변경 (grid-3/grid-4 → dash-left/dash-right 2열 구조, 가장 큰 작업)
- [ ] **Phase 4** — 데이터 수치 검증 (js/data.js 각 패널 수치 Figma 원본과 일치 여부)
- [ ] **Phase 5** — 컴포넌트 디테일 보정 (도넛 레전드 칩 크기, 헤더 버튼 높이 32px 등)

---

## C. 추가 화면 (Figma 시안에 있는 것)

- [ ] **자산 생애주기 모니터링** — 도입 / 운영 / 점검 / 유휴 / 폐기 단계별 현황.
- [ ] **조직·자산 통계 리포트** — 조직별 비교·순위, 연도별 추이 차트.
- [ ] **일반자산 취득** — 취득 등록 워크플로.
- [ ] **차량 운행기록부 등록·삭제**.
- [ ] **결산전 수행작업**.
- [ ] **건물 자산 생애주기(Lifecycle) 관리**.

---

## D. 디자인·데이터 다듬기

- [ ] **NH 로고** 정확한 형태/색으로 교체(현재는 NH 빌딩 아이콘).
- [ ] **Pretendard 웹폰트** 적용 여부 결정(현재는 시스템 글꼴).
- [ ] 분류·부서·자산명을 실제 NH 환경에 맞게 다듬기.
- [ ] 자산 건수 조정(시연 분위기에 맞게).

---

## E. 마무리

- [ ] **시연 시나리오 스크립트** — 어떤 순서로 어떤 화면을 보여줄지 구체적 흐름 정리 (시연_자막.txt 초안 있음).
- [ ] 불필요한 캡처 이미지(`fig_*.png`, `figma_overview.png`) 정리 여부 결정.

---

## F. 보안 — 비밀 프로젝트 화면 (선택, 나중에)

> 2026-06-04 비밀번호 평문을 SHA-256 해시로 전환 완료(소스 노출 제거). 아래는 **추가로 더 강화하고 싶을 때**의 후속 작업. 상세는 [HASHGEN.md](HASHGEN.md) 참고.

- [ ] **강한 비밀번호로 교체** — 현재 `78963`은 5자리 숫자(10만 가지)라 해시를 가진 사람의 무차별 대입엔 약함. 길고 영문·숫자·기호 섞인 비밀번호로 바꾸면 사실상 대입 불가. (HASHGEN.md 방법 A/B로 새 해시 생성 → `js/app.js`의 `PW_HASH`만 교체)
- [ ] (원할 때) **키 스트레칭** — 해시 반복(iteration) 적용해 대입 1회 비용을 높이기. (단, 순수 프론트엔드 한계상 근본 방어는 아님)
- [ ] (진짜 보안 필요 시) **서버 인증 도입** — 순수 프론트엔드에선 검증 로직이 브라우저에 다 있어 "완벽한 비밀"은 불가능. 민감정보 보호가 목적이면 별도 서버 인증이 필요.
