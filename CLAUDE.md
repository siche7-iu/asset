# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트 개요

- **무엇**: 고정자산관리시스템(Fixed Asset Management System)의 **시연용 프로토타입**.
- **목적**: 실제 운영 시스템이 아니라, 시연(데모)으로 보여주기 위한 화면을 만드는 것이 목표.
- **실제 개발**: 운영 버전은 별도의 내부 보안 환경에서 진행됨. 따라서 이 프로토타입은 "운영급 구조"보다 "쉽게 실행하고 보여주기"를 우선한다.
- **사용자**: 개발에 익숙하지 않은 기획자. 설명은 한국어로, 전문 용어를 최소화한다. 큰 작업은 작은 단위로 쪼개서 진행한다.

## 기술 구성 (의도적으로 단순하게)

- 순수 **HTML + CSS + JavaScript**만 사용한다. 외부 라이브러리·빌드 도구·서버가 **전혀 필요 없다**.
- 차트는 라이브러리 없이 간단한 SVG/CSS로 직접 그린다(인터넷 없이도 동작하게 하기 위함).
- 자산 데이터는 `js/data.js`에 코드로 직접 넣어 둔 샘플 데이터다(데이터베이스 없음). 매번 같은 데이터가 보이도록 무작위 없이 규칙으로 생성한다.

## 실행 방법

- 설치할 것이 없다. **`index.html` 파일을 더블클릭**하면 기본 웹 브라우저에서 바로 열린다.
- (선택) 명령으로 열기: PowerShell에서 `start index.html`
- 코드를 고친 뒤에는 브라우저에서 **새로고침(F5)** 하면 바뀐 내용이 반영된다.

## 화면/파일 구조

```
index.html        메인 화면. 왼쪽 그룹형 메뉴(사이드바) + 상단 헤더 + 콘텐츠 영역.
css/style.css     전체 디자인. 맨 위 :root 의 색상/치수 토큰은 DESIGN.md 규칙을 그대로 옮긴 것이다.
js/data.js        샘플 자산 데이터. window.APP_DATA.assets 배열로 제공한다.
js/app.js         화면 동작 전부. 메뉴 전환, 대시보드 계산/차트, 목록/검색, 상세/이력 표시.
DESIGN.md         디자인 시스템 규칙(색상·글자·간격·구성요소·메뉴). 디자인의 단일 기준.
HANDOFF.md        현재 상태 인수인계(완료 내용 + 이어서 작업하는 방법).
TODO.md           다음에 할 일 목록(체크리스트).
```

> **디자인은 `DESIGN.md`를 단일 기준으로 삼는다.** Figma 시안(NH 고정자산관리)에서 정리한 규칙이며,
> 화면을 만들거나 고칠 때는 `style.css` 의 `:root` 토큰과 `DESIGN.md` 를 기준으로 한다.

### 동작 방식(큰 그림)

- 화면은 페이지 이동 없이 한 페이지 안에서 **세 개의 영역을 보였다 숨겼다** 하며 전환한다.
  - `#view-dashboard` (대시보드) · `#view-list` (자산 목록) · `#view-detail` (자산 상세)
  - 왼쪽 메뉴 버튼(`.nav-item`)이 `showView()`를 호출해 전환한다.
- `js/app.js`는 시작할 때 `window.APP_DATA.assets`(자산 목록)를 읽어:
  - **대시보드**: 합계/개수를 계산하고, 분류별·부서별은 막대그래프, 상태별은 도넛 그래프로 그린다.
  - **목록**: 표로 그리고, 검색어와 분류 선택으로 걸러서 다시 그린다.
  - **상세**: 목록의 행을 클릭하면 해당 자산의 정보와 변경 이력(타임라인)을 보여준다.
- 자산 한 건의 데이터 형태(`js/data.js`):
  `id`(자산번호), `name`(자산명), `category`(분류), `model`(모델), `department`(사용부서),
  `owner`(사용자), `location`(위치), `status`(상태: 사용중/유휴/수리중/폐기예정),
  `acquireDate`(취득일), `price`(취득금액), `history`(변경 이력 배열: `date`, `type`, `detail`).

## 작업 규칙 (중요)

- 모든 대화·설명은 **한국어**로, 전문 용어는 최소화한다.
- 큰 작업은 **작은 단위로 쪼개서** 진행한다.
- **작업을 할 때마다 이 파일(`CLAUDE.md`) 맨 아래 "작업 로그"에 한 줄로 간단히 기록**한다(날짜 + 무엇을 했는지, 쉬운 말로).
- **기본 모델은 Sonnet을 쓴다.** 빠르고 비용 효율적인 Sonnet을 기본으로 사용한다. 아주 복잡한 설계·깊은 분석·어려운 디버깅 등 **꼭 필요한 경우에만** 그 작업에 한해 Opus를 쓰고(주로 Opus 보조 에이전트에게 위임하는 방식), 끝나면 다시 Sonnet으로 돌아온다.

### 자율모드 (항상 활성화 — 별도 선언 불필요)

- **자율모드는 기본값이다.** 대화 시작부터 자율모드로 동작한다.
- 묻지 않고 최선이라 판단되는 방향으로 바로 실행한다.
- 결과만 간결하게 보고한다. 방법론·계획·확인 질문은 생략한다.
- **멈추는 경우는 오직**: 되돌리기 어려운 파괴적 작업(파일 삭제·덮어쓰기 등), 또는 방향 자체가 불명확해 어떤 판단도 불가능할 때.

### 세션 시작 브리핑 (그날 첫 대화에서만)

- 대화가 새로 시작될 때, 첫 응답에서 TODO.md·HANDOFF.md·작업 로그를 읽고 **"오늘 뭐부터 하면 좋을지" 3줄 이내로 간단히 브리핑**한다.
- 브리핑 후 바로 지시를 기다린다. 허락 없이 먼저 실행하지 않는다.
- 같은 세션 안에서 두 번째 대화부터는 브리핑을 반복하지 않는다.

### 개발 원칙 (코드를 작성·수정할 때)

1. **먼저 주변을 본다.** 코드를 작성하거나 고치기 전에, 반드시 **고칠 부분의 주변 코드 구조를 먼저 분석**한다. (어떤 함수·변수·규칙을 따라야 하는지 파악한 뒤에 손댄다.)
2. **예외 상황부터 적는다.** 해결책을 바로 내놓지 말고, 발생할 수 있는 **예외 상황(Edge Case)을 최소 3가지 이상 먼저 목록으로** 정리한다. (예: 데이터가 비었을 때, 값이 너무 클 때, 예상과 다른 형식이 들어올 때 등.)
3. **끝나면 스스로 검토하고 보고한다.** 코드가 완성되면 **스스로 코드 리뷰**를 하고, **보안·성능상 문제가 없는지 검증한 결과를 쉬운 말로 요약해서 보고**한다.

---

## 작업 로그 (변경 내역)

- **2026-05-27** — 프로젝트 첫 구성. 시연용 웹 화면 뼈대를 만들고, 샘플 자산 데이터(약 70건)를 넣었습니다. 화면 3개(대시보드 / 자산 목록·검색 / 자산 상세·이력)를 동작하게 만들었습니다. `index.html`을 더블클릭하면 바로 열립니다.
- **2026-05-27** — Figma 시안(NH 고정자산관리)을 읽어 디자인 시스템 규칙을 `DESIGN.md`로 정리했습니다. 그 규칙(남색 사이드바·파랑 포인트·KPI 카드 등)을 화면에 적용해, ① NH 로고와 그룹형 사이드바 메뉴(WORKSPACE/자산 관리/예산·세무/결산·감사/시스템), ② 흰색 상단 헤더(제목·검색·자산 등록 버튼), ③ KPI 6칸 + 차트 대시보드로 새단장했습니다. 동작 화면은 대시보드·원장관리(목록·상세)이고 나머지 메뉴는 "준비 중"으로 표시됩니다.
- **2026-05-27** — 완료 내용과 현재 상태를 `HANDOFF.md`로, 다음에 할 일을 `TODO.md`로 정리했습니다.
- **2026-05-27** — 프로젝트를 git으로 관리하기 시작하고 GitHub(`siche7-iu/asset`)에 처음 올렸습니다. 도구 임시폴더(`.claude`, `.omc`)는 `.gitignore`로 제외했습니다. 이제 코드를 고친 뒤 `git add -A` → `git commit` → `git push`로 변경 내용을 GitHub에 백업/공유할 수 있습니다.
- **2026-05-27** — Playwright를 설치했습니다. 화면 자동 스크린샷(대시보드·자산목록·자산상세)과 기본 동작 테스트(6개) 작성 완료. `npm test`로 실행하면 6개 모두 통과합니다. 스크린샷은 `test-results/` 폴더에 저장됩니다.
- **2026-05-27** — 코드 작업 시 지켜야 할 "개발 원칙" 3가지(① 주변 코드 먼저 분석 ② 예외 상황 3가지 이상 먼저 정리 ③ 완료 후 스스로 코드 리뷰·보안/성능 검증 보고)를 `CLAUDE.md`의 "작업 규칙" 안에 추가했습니다.
- **2026-05-27** — 대시보드 화면을 새 시안 이미지(`기본화면_대시보드.png`)에 맞춰 전면 새단장했습니다. AI 인사이트 배너, KPI 6칸(원형 게이지 포함), 노후 위험도 도넛, 교체시기 타임라인, 한국 지도(지역별 현황 + 서울/강원/경상/전라/제주), 위험자산 TOP5·최근 이슈·계약/점검 일정·노후도 분석, 우하단 AI 버튼을 추가했습니다. 사이드바 아이콘을 그림 아이콘으로 바꾸고 `기타관리` 메뉴를 추가, 헤더는 탭(고정자산관리시스템/지역별 현황/현장 점검) + 리포트 생성 버튼으로 변경했습니다. 화면의 큰 숫자는 시안 그대로의 요약 수치이며, 원장관리(목록·검색·상세)는 그대로 동작합니다. 시안과 비교하려고 브라우저로 화면을 실제 렌더링해 확인했습니다.
- **2026-05-27** — 기본 모델을 Sonnet으로 바꿨습니다(설정 파일 `model: sonnet`). 꼭 필요한 어려운 작업만 Opus를 쓰고 끝나면 Sonnet으로 돌아오도록 "작업 규칙"에 추가했습니다.
- **2026-05-27** — SPA URL 라우팅을 추가했습니다. 각 메뉴를 클릭하면 브라우저 URL 표시창에 경로(`#/dashboard`, `#/list`, `#/detail/자산번호` 등)가 표시됩니다. 브라우저 뒤로/앞으로 버튼도 동작합니다. `file://`로 직접 여는 환경에서도 안정적으로 동작하도록 해시 방식(`#/경로`)을 사용했습니다.
- **2026-05-28** — KPI 레이아웃을 6열→3×2 배열로 변경하고 "내 업무 현황" 카드 CSS(`.kpi-section`, `.my-task-card` 등)와 "지역별 관리 현황" 2열 레이아웃 CSS(`.map-left`, `.map-right`)를 추가했습니다.
- **2026-05-28** — 지도 CSS를 새 말풍선 구조에 맞게 교체했습니다. 구 클래스(`.region-detail`, `.region-pin`, `.bubble`)를 제거하고 신규 클래스(`.region-marker`, `.callout`, `.callout-exp`, `.callout-cmp`, `.cb-pin`)를 추가했습니다.
- **2026-05-28** — 지도 핀 위치(data.js regions x/y)를 새 SVG(viewBox 0 0 480 580) 기준으로 재조정했습니다. 서울(38,44)·강원(71,22)·경상(78,58)·전라(30,68)·제주(39,88). Playwright로 스크린샷 검증 완료(test-results/map-check.png).
- **2026-05-28** — 대시보드 디자인 디테일 수정 완료. ① 사이드바 "섬 효과"(body 배경 #CDD3DE, sidebar border-radius:18px·여백·높이 조정, layout padding/gap), ② .main 둥근 카드(overflow:clip으로 sticky 헤더 유지), ③ 사이드바 폭 축소(272→220px), ④ 리포트 생성 버튼 파란색, ⑤ NH 텍스트 로고 교체(SVG 아이콘 제거).
- **2026-05-28** — 자산 등록 모달을 구현했습니다. 원장관리 목록 화면에 "자산 등록" 버튼을 추가하고, 클릭 시 자산명·분류·사용부서 등 9개 필드를 입력할 수 있는 모달이 열립니다. 필수 항목 미입력 시 빨간 테두리로 안내하고, 등록 완료 시 자산 목록 맨 위에 바로 추가되어 목록에서 확인할 수 있습니다.
- **2026-05-28** — 자산 상세 화면을 보강했습니다. 감가상각 정보(내용연수·사용연수·잔여연수·연간감가상각액·잔존가액·현재 장부가·진행률 게이지), 담당자 연락처 카드(분류별 자동 표시), 첨부파일 플레이스홀더를 추가했습니다.
- **2026-05-28** — 소형 버그 4건 수정. ① 사이드바 브랜드 텍스트 "고정자산신탁관리"→"고정자산관리" 수정, ② "준비 중" 메뉴 여러 개 동시 활성화 버그 수정(data-title로 구분), ③ 헤더 탭 "고정자산관리시스템" 항상 활성 고정, ④ 노후 위험도 도넛 SVG를 반응형 크기(100%)로 변경.
- **2026-05-28** — style.css 4곳 수정: ① `.btn-outline` 그레이 테두리 복원, ② `.ai-banner` 필(pill) 모양 + 그라데이션 보더(border-radius:40px), ③ `.kpi-badge` align-self: flex-end(우측 정렬), ④ `.risk-donut` 반응형(min(168px,45%)+aspect-ratio) 및 `.panel-body.card-white`에 flex column 추가.
- **2026-05-28** — 사이드바 CSS 3곳 개선. ① 활성 메뉴 스타일(전체 파랑→반투명 흰색, 아이콘만 파랑 강조), ② NH 로고 박스·텍스트 폰트 강화, ③ 사이드바 토글 버튼·접힌 상태(`.sidebar.collapsed`) CSS 추가.
- **2026-05-28** — 대시보드 레이아웃 2곳 개편(index.html). ① KPI 6칸을 `kpi-section`으로 감싸고 오른쪽에 "내 업무 현황" 카드(할 일 4개) 추가. ② "지역별 관리 현황" 패널 내부를 지도(좌)·노후도 분석(우) 2열로 변경, 하단 grid-4에서 노후도 패널 제거 → grid-3으로 변경. aging-bars/aging-stats id 중복 없이 이동 완료.
- **2026-05-28** — 사이드바 HTML·JS 개선 완료. ① aside에 id=sidebar 추가 + 토글 버튼(더블 쉐브론) 추가. ② 11개 메뉴 아이콘 SVG 전체 교체. ③ 모든 nav-item 텍스트를 span.nav-item-label로 감싸 접힌 상태 숨김 처리. ④ side-foot 텍스트도 span.side-foot-text로 감쌈. ⑤ toggleSidebar() 함수 app.js 맨 끝에 추가.
- **2026-05-28** — 대시보드 진입 애니메이션 추가. ① AI배너→KPI카드(순차)→내업무현황→가운데 패널 3개→하단 패널 3개 순서로 fadeUp 등장(총 ~1초). ② KPI 숫자 카운터(0→최종값, 700ms). ③ KPI 게이지 원호 성장(0→최종 arc). ④ Aging 바 성장(width 0→최종%). 재방문 시에도 매번 재생.
- **2026-05-28** — SIDEBAR_REDESIGN.md 반영: ① NH 그린 박스 제거→라인 홈 아이콘으로 교체. ② 활성 메뉴 아이콘박스 파랑→흰색 반전(아이콘 남색). ③ 사이드바 토글 버튼 외부→내부 우상단(둥근 사각형 반투명). ④ 하단 링크 둥근 사각형 배경 추가. ⑤ 메뉴 7개 아이콘 교체(재물조사·고정투자예산·세무관리·결산관리·회계감사자료·기타관리·시스템관리). ⑥ KPI카드/내업무현황/패널이 4열 그리드로 동일 열 폭에 정렬되도록 수정.
- **2026-05-28** — AI Agent 화면 구현 완료. 사이드바 "AI Agent" 클릭으로 진입(`#/ai-agent`), 3열 레이아웃(추천질문/채팅/분석패널)입니다. 추천 질문 1·2번 클릭만으로 시나리오 1(본점영업부 노후 PC 5건) 또는 2(보험 만료 임박 차량 6건) 자동 진행. "분석중" 인디케이터가 "자산 데이터 조회→정책 기준 검토"(또는 "보험 계약 조회→만기 기준 검토")로 바뀌면서 우측 6단계가 순차 활성화, 약 2.1초 후 자산 목록 테이블·관련 자산 칩·영향 비용 강조 박스·추천 조치가 나타납니다. 자산 행 클릭 시 우측 패널이 자산 상세(감가상각 게이지/보험 D-day 진행바·이력·AI 진단·액션 버튼)로 슬라이드 전환됩니다. Playwright 테스트 4개 통과(`tests/ai-agent-check.spec.js`).
- **2026-05-28** — 대시보드 정렬 2건 수정. ① `.sidebar { position: relative; }` 중복 선언 제거(sticky가 덮혀서 10px 아래로 밀리던 버그). ② 대시보드 4열 그리드 제거(내 업무 현황 하단 공백 해소, 중앙·하단 패널이 100% 폭에서 각 1/3씩 차지). ③ 헤더 탭 클릭 시 해당 탭이 직접 활성화되도록 수정("지역별 현황·현장 점검" 탭 클릭 반응, 사이드바 메뉴는 기존대로 "고정자산관리시스템" 탭 유지).
- **2026-05-28** — AI Agent 화면 진입 애니메이션 추가. ① 페이지 헤더→좌측 추천질문/즐겨찾기→중앙 채팅카드→우측 분석단계/관련자산/영향비용/추천조치 순으로 fadeUp 등장(약 1초). ② 추천 질문 6개·분석 단계 6개 항목도 60ms 간격 stagger로 순차 등장. ③ 에이전트 인사 메시지는 진입 약 0.55초 후 pop-in 애니메이션(살짝 확대+페이드)으로 등장. ④ `+ 새 분석 시작` 버튼 클릭 시 인사 메시지가 다시 fade-in으로 재등장(0.2s 지연). 매 진입마다 재생(view active 클래스 재토글).
- **2026-05-28** — 헤더 납작 버그 수정 + 알림 팝업 구현. ① 스크롤 구조 개선: `.main`은 `overflow:hidden`으로 고정, `.content`만 `overflow-y:auto`로 내부 스크롤. topbar는 sticky 불필요(자연히 상단 고정). `window.scrollTo` → `.content.scrollTop` 수정. ② `--header-h` 66px→76px로 높이 보완. ③ 헤더 벨 아이콘 클릭 시 알림 말풍선(알림 제목·시간·해제/지우기 버튼·더보기·전체 알림 보기) 구현. 배지 숫자 연동, 모두 지우기·개별 지우기·해제 동작.
- **2026-05-29** — 대시보드 HTML 구조 개편. ① 헤더 탭 '지역별 현황' → '자산현황' 변경. ② KPI 섹션에서 my-task-card 제거(KPI 6칸이 전체 폭 차지). ③ 아바타 버튼에 '내 업무 현황' 팝업(mywork-popup) 추가(알림 팝업과 상호 닫힘 연동). ④ 지도 패널을 지도 단독으로 단순화. ⑤ 하단 grid-3 → grid-4 변경 후 자산 노후도 분석 패널 이동 추가.
- **2026-05-29** — style.css 대시보드 CSS 6곳 변경. ① kpi-section→1행/kpi-row→6열 그리드. ② my-task-card·mth-title·mt-tab 등 내 업무 현황 카드 CSS 전부 제거. ③ 내 업무 현황 팝업(.mywork-popup) CSS 추가(알림 팝업 바로 뒤). ④ 지도 2열 레이아웃(map-inner·map-left·map-right·map-sep·map-aging-title) CSS 제거, .panel-body.card-white .map-box 추가. ⑤ aging-bars·aging-stats 스타일 분리 선언 정리. ⑥ 미디어쿼리 1400px에 kpi-row 3열 추가.
- **2026-05-29** — 4K 전체화면 대응 개선: ① `.risk-donut`을 `clamp(120px, 38%, 230px)`로 설정해 패널 폭에 비례해 도넛이 커지도록 수정. 도넛-범례 gap도 `clamp(12px,3%,24px)`으로 반응형 적용. ② `.repl-timeline`에 `flex:1` 복구 + 각 `li`를 `flex:1`로 설정 + `.repl-card`에 `flex:1` 추가해, 카드 4개가 패널 높이 전체를 균등 분할하며 채우도록 수정(gap:10px 고정).
- **2026-05-29** — 대시보드 레이아웃·스크롤 3건 개선. ① `.content`를 flex 컬럼으로 변경 + 스크롤바 hover 시에만 노출(평상시 투명). ② `#view-dashboard.active`를 `flex:1` 컬럼으로 변경해 하단 grid-4가 남은 뷰포트를 자동 채움. ③ grid-4 패널에 내부 스크롤(`overflow-y:auto` + `min-height:0` + hover 시에만 4px 스크롤바) 추가. ④ 1400px 미디어쿼리에서 `grid-template-rows:auto` 리셋. ⑤ 하단 4개 카드 진입 애니메이션(dashFadeUp 0.5~0.71s) 추가. 기존 죽은 코드(`grid-3:nth-child(4)`) → `grid-4` 셀렉터로 수정.
- **2026-05-29** — 헤더 구분선(tb-divider) 정렬 수정. `display: block; align-self: center; flex-shrink: 0;` 명시적 추가로 flex 컨테이너 내 수직 중앙 정렬 보장.
- **2026-05-29** — AI Agent 상세 패널 스크롤바 개선. `.ai-right-detail`에 투명 스크롤바 설정 + 호버 시만 얇게(4px) 노출. `scrollbar-width: thin; scrollbar-color: transparent` + webkit 커스텀 스크롤바 적용.
- **2026-05-29** — [내 업무 현황] · 알림 팝업 동시 노출 버그 수정. 알림 버튼 클릭 시 내 업무 현황 팝업을 먼저 닫도록 수정(기존엔 내 업무→알림 방향만 처리됐고 반대 방향은 누락). 두 팝업이 항상 상호 배타적으로 동작.
- **2026-05-29** — AI Agent 분석 단계 로딩 애니메이션 추가. 추천 질문 클릭 시 현재 진행 중인 단계 번호 주위에 파란 호(arc)가 도는 스피너 표시. conic-gradient + mask로 꼬리가 부드럽게 페이드되는 원호 효과. 단계 완료 시 스피너 사라지고 다음 단계로 이동. 마지막(6번) 완료 시 전체 스피너 제거.
- **2026-05-29** — 하단 grid-4 카드 상단 여백 축소. 위험 자산 TOP5·최근 이슈·계약/점검 일정 흰 박스 상단 패딩 18→9px, 자산 노후도 분석의 총 노후 자산/분기 교체 권고 박스 상단 패딩 16→8px.
- **2026-05-29** — 사이드바 로고 폰트 14.5→19px, 로고 이미지 22×18→30×24px으로 확대.
- **2026-05-29** — 사이드바 로고 레이아웃 수정. 1행(아이콘+NH 고정자산관리), 2행(Fixed Asset Management) 2단 구조로 변경(원본 디자인 일치).
- **2026-05-29** — 자산 노후도 분석 패널 높이 정렬. panel-body에 card-white 추가해 단일 흰 박스로 통합, aging-bars·aging-stats 개별 배경 제거. 좌측 3개 카드와 높이 일치.
- **2026-05-29** — 하단 카드 높이 문제 수정. grid-3이 flex-shrink:0으로 427px를 점유해 grid-4에 31px만 남던 문제 → grid-3:grid-4를 flex 3:2 비율 분할로 변경(grid-3≈275px, grid-4≈183px). content padding-bottom 40→10px 축소, dashboard 명시적 height 조정 → grid-4 bottom이 사이드바 bottom과 10px 차이로 거의 일치.
- **2026-05-29** — AI INSIGHT 배너 호버 애니메이션 추가. 마우스오버 시 흰색 광택(::before)이 왼→오로 흐르는 shimmer 효과(2s infinite) + 파란색 box-shadow 등장. skewX(-20deg)로 대각선 광택 연출.
- **2026-05-29** — 지역별 관리 현황 말풍선 잘림 수정. grid-3 overflow-y:auto로 인해 절대위치 callout이 clipping되던 문제 → 지도 패널(:first-child)과 panel-body에만 overflow:visible 예외 적용.
- **2026-05-29** — KPI 카드 마우스오버 효과 추가. 호버한 카드는 1.04배 확대+그림자 강조, 나머지 카드는 0.96배 축소+흐림. 순수 CSS(:has() + transition)으로 구현.
- **2026-05-29** — 노후 위험도 분포(도넛) · 2차 시기 도래 자산(타임라인) 마우스오버 툴팁 추가. 검정 말풍선으로 라벨·수치 표시, 화면 끝 경계 체크 포함.
- **2026-05-29** — KPI 카드 hover scale 버그 수정. animation-fill-mode: both→backwards 변경. both가 애니 종료 후 transform:none을 frozen 유지해 hover scale을 덮어쓰던 문제 해결. JS mouseenter/mouseleave 방식으로 형제 카드 축소(kpi-dimmed) 확실히 동작.
- **2026-05-29** — 자산 노후도 분석 막대 차트 마우스오버 툴팁 추가. 각 구간 행 hover 시 검정 말풍선으로 구간명·비율·자산 수 표시. data.js bars에 count 필드 추가.
- **2026-05-29** — AI INSIGHT 배너 hover 광택 효과 강화. 광택 불투명도 0.5→0.88, 너비 55%→70%, 속도 2s→1.1s linear, 박스 그림자+filter:brightness 추가.
- **2026-05-29** — 지역별 관리 현황 말풍선 잘림 버그 수정(overflow:visible이 :first-child 적용→:last-child로 교체). 지도 상하좌우 중앙 정렬 + 패딩 18→10px 축소로 지도 크기 확대.
- **2026-05-29** — AI Agent 채팅 이력 누적 버그 수정. 새 질문 시 이전 답변 말풍선을 모두 지우던 코드(answer 제거 3줄) 삭제. 이제 질문·답변이 채팅창에 쌓임(분析중 인디케이터만 제거).
- **2026-05-29** — AI Agent 레이아웃 고정 + 내부 스크롤 구현. view-ai-agent.active에 고정 높이(100vh 기준) + overflow:hidden 적용. ai-layout이 남은 공간 채우도록 flex:1. 좌측·우측 패널 각각 내부 스크롤. 채팅 헤더('고정자산 Agent')·입력창은 상단·하단에 고정(flex-shrink:0), 대화 본문만 내부 스크롤. 외부 콘텐츠 스크롤 0 확인.
- **2026-05-29** — 알림 팝업 클릭 → 화면 이동 + 읽음 처리 구현(TODO A 완료). 항목별 data-nav 추가(계약만료→자산상세, 점검→목록, 결재→준비중). 하단 패널(위험자산TOP5·이슈·일정) 행 hover 효과 추가.
- **2026-05-29** — 지역별 관리 현황 지도 핀·말풍선 전면 개선. ① 말풍선을 position:absolute로 변경해 핀 크기(20×20px) 기준으로만 transform 계산 → 확장 말풍선 열릴 때 핀 위치 이동 버그 수정. ② 서울·전라·제주(dir-left) 말풍선이 핀 왼쪽에, 강원·경상(dir-right) 말풍선이 핀 오른쪽에 배치. 꼬리가 핀 중앙을 향하도록 top:50% 정렬. ③ 상세(확장) 말풍선 꼬리 숨김. ④ 각 지역 핀 좌표 지도원본 기준으로 재조정.
- **2026-05-29** — 2차 시기 도래 타임라인 카드 늘어남·스크롤 버그 수정. li에 flex:1+max-height:72px 적용(과도한 성장 차단), justify-content:space-between으로 남는 공간을 간격에 분배. 카드 패딩 14px→8px, 합계 행 패딩 18px→12px 축소. 1920×1080·1440×900 양쪽 4카드+합계 행 모두 표시, 스크롤 없음 확인.
- **2026-05-29** — 지도 말풍선 4건 위치 버그 수정. ① 강원·경상 상세 말풍선 → expDir:"left" 적용(우측 패널 밖 잘림·경상 말풍선 겹침 해소). ② 전라·제주 상세 말풍선 → expAbove:true 적용(전라 활성 시 제주 말풍선 올라옴·제주 활성 시 하단 잘림 해소). CSS .exp-dir-left/.exp-above 클래스 추가, data.js·app.js 연동.
- **2026-05-29** — Figma 원본(node 209:2622) MCP 분석 완료. 현재 구현과 Figma 간 차이를 `FIGMA_SYNC_PLAN.md`(5단계 수정 계획)로 정리. `DESIGN.md`를 Figma 실측값(치수·간격·데이터 수치)으로 전면 갱신. 백업 `backup/영상시연용_260529/`을 기준점으로 기록.
- **2026-06-01** — Figma 재분석(채널 arw2v5xz, node 209:2622) 결과를 .md 파일에 기입. DESIGN.md: KPI1 증감 방향(▲→▼) 수정, 도넛 MEDIUM 오타 수정, AI배너 본문 텍스트·이슈 5건·계약일정 3건 상세 확정. FIGMA_SYNC_PLAN.md: Section C 수치 항목 전부 ✅ 확인 완료로 갱신.
- **2026-06-01** — AI Agent 채팅 퍼지 매칭 추가. 오타·띄어쓰기 오류 입력 시에도 3단계(완전일치→정규화일치→키워드스코어링) 매칭으로 동일 결과 표시.
- **2026-06-01** — 보고서 화면 구현. AI Agent 답변 말풍선 우측 하단에 [보고서 작성] 버튼 추가, 클릭 시 `#/report` 화면으로 이동. 보고서 화면은 4개 유형 카드(시나리오별 자동 선택) + 로딩 후 보고서 본문 자동 생성. 시나리오2(차량 보험 만료) 전용 '차량 보험 만료 현황 보고서' 6섹션(요약/현황표/위험분석/예산영향/우선조치/결론) 구현.
- **2026-06-01** — 지역별 관리 현황 지도 영역 호버 기능 추가. ① 핀·말풍선뿐 아니라 도(道) 전체 면적 위에 마우스를 올려도 상세 말풍선 표시(투명 SVG 오버레이 히트 영역). ② 마우스를 떼면 0.5초 유지 후 부드럽게 fade-out(등장 0.15s, 소멸 0.35s CSS transition). ③ 다른 지역으로 빠르게 이동하면 타이머 취소되어 즉시 전환. ④ SVG 히트 영역 hover 시 파란 그림자 노출 버그 수정(opacity 항상 0 고정).
- **2026-06-01** — 로고 클릭 시 대시보드 이동 + AI Agent FAB 버튼 shimmer 애니메이션 추가. ① `.brand` 클릭 → 어느 화면에서든 대시보드로 이동. ② FAB 버튼에 overflow:hidden + ::before 광택 추가, hover 시 AI INSIGHT 배너와 동일 계열 shimmer(1s, 흰 대각선 왼→오)와 파란 glow 그림자 효과.
- **2026-06-01** — 대시보드 진입 애니메이션 재실행 수정. `triggerDashboardAnimations()`에 CSS 애니메이션 리셋(active 클래스 제거→강제 reflow→재추가)을 추가해, 대시보드에서 로고·메뉴를 클릭해도 항상 처음 진입처럼 패널 등장 애니메이션이 재생됨.
- **2026-06-01** — FAB 버튼 아이콘을 AI INSIGHT 배너와 동일한 이미지(`ai_insight.svg`)로 교체. FAB CSS 배경 그라데이션 제거(SVG 자체 원형 그라데이션 사용).
- **2026-06-01** — 지역별 현황 말풍선 z-index 버그 수정. 경상 상세 말풍선 위로 전라 기본 말풍선이 올라오던 문제 → `setActive()`에서 활성 마커 z-index를 10→20으로 올리도록 수정.
- **2026-06-01** — AI Agent 입력창 맞춤법 빨간줄 제거(`spellcheck="false"` 추가).
- **2026-06-01** — 시연 자막 텍스트 파일(`시연_자막.txt`) 생성. 대시보드 10줄·AI Agent 시나리오1 8줄·시나리오2 7줄 구성. 하단 4개 패널 자막을 한 문장으로 통합.
- **2026-06-02** — Figma 동기화 Phase 1 완료(헤더 탭 "자산현황"→"지역별 현황", 타임라인 제목 "2차 시기 도래"→"교체 시기 도래"). HANDOFF.md·TODO.md를 6월 1일까지의 작업 내용으로 현행화.
- **2026-06-02** — 1920×1080 대응 CSS 추가. `@media (max-height: 1100px)`로 고정 요소(AI배너·KPI 카드·게이지·패딩·그리드 간격) 압축 → 그리드 패널에 약 68px 공간 확보. 백업 `backup/260602_1920수정전/` 생성.
- **2026-06-02** — 대시보드 레이아웃 Figma Phase 3 완료. grid-3(3균등)+grid-4(4균등) → dash-cols 2열 구조(좌67%+우33%)로 전환. 좌상단(도넛·타임라인)/좌하단(TOP5·이슈·일정)/우상단(지도, 70%)/우하단(노후도분석, 30%). 애니메이션·max-height 미디어쿼리 셀렉터 일괄 업데이트.
- **2026-06-02** — 대시보드 패널 비율 재조정. 좌측 상:하 5:3→1:1, 우측 지도:노후도분석 7:3→3:2. 1080p에서 노후도분석 패널 및 하단 패널 콘텐츠 가시성 개선.
- **2026-06-02** — 사이드바 이너스크롤 개선. 사이드바 전체 스크롤 → 메뉴 영역만 이너스크롤로 변경. 상/하단 라운드(18px) 항상 유지. 스크롤바는 4px 반투명 흰색 디자인.
- **2026-06-02** — 1080p 대시보드 스크롤 모드 추가. max-height:1100px에서 #view-dashboard 고정 높이 제거(height:auto) + 패널별 min-height(도넛420·하단340·지도520·노후도260) 설정. 높이 충분한 해상도는 기존 꽉 채운 레이아웃 유지.
- **2026-06-02** — 우측 패널 구조 개선. "지역별 관리 현황"과 "자산 노후도 분석"을 두 개의 독립 패널에서 하나의 회색 박스로 통합(Figma 원본 일치). 지도 흰 배경 이중 겹침 수정: `dr-map-card`에 `container-type:size`를 적용해 `min(100cqw,100cqh)` 정사각형 map-box가 흰 카드를 딱 채우도록 수정.
- **2026-06-02** — 1080p 노후 위험도 분포 패널 상/하 여백 절반 축소. dash-left-top min-height 420→340px. dash-left-bot을 flex:1로 변경해 남은 공간 자동 채움(TOP5·이슈·일정 패널 더 넓어짐).
- **2026-06-02** — 자산 노후도 분석 흰 박스 상단 여백 통일. aging-bars 16→18px, aging-stats 8→18px + justify-content:center→flex-start로 수정. center 정렬이 60px 여백을 만들던 버그 수정, 18px로 통일.
- **2026-06-02** — 자산 노후도 분석 레이아웃 수정. 막대차트(aging-bars)·통계표(aging-stats) 배열을 상/하(flex column) → 좌/우(grid 2열)로 변경해 Figma 원본과 일치.
- **2026-06-02** — 지역별 관리 현황 지도 상/하 여백 절반 축소. `dr-map-card` padding 10px → 5px, container query 계산값 -20px → -10px 조정.
- **2026-06-02** — 지도 카드 상/하 여백 제거. `dr-map-card`에 `flex:none; aspect-ratio:1/1` 적용해 카드 자체를 정사각형으로 변경. gap 51px → 10px(1440), 10px 유지(1920).
- **2026-06-02** — 알림 팝업 꼬리 위치 수정. `.np-arrow right: 88px → 70px`으로 조정해 꼬리가 벨 아이콘 수직 중앙과 정렬되도록 수정.
- **2026-06-02** — 내 업무 현황 말풍선 버튼 간격 수정. `.mywork-popup top: 8px → 14px`으로 늘려 버튼-꼬리 시각적 간격을 2px → 8px로 확보.
- **2026-06-02** — 하단 패널 더보기/필터 아이콘 흰 원형 배경 적용. `.dots`에 `background:#fff; box-shadow` 기본값 추가(Figma 디자인 원본 일치).
- **2026-06-02** — 하단 3개 패널(위험 자산 TOP5·최근 이슈 내역·계약/점검 일정) 텍스트 말줄임 적용. 해상도가 좁아질 때 제목·설명이 한 줄로 유지되며 말줄임(…) 표시. 마우스오버 시 기존 검정 툴팁으로 전체 텍스트 표시(실제 잘린 경우에만).
- **2026-06-02** — 하단 3개 패널 가로 폭 불균등 버그 수정. `dash-left-bot` 패널에 `min-width:0; overflow:hidden` 추가 → 3개 패널이 정확히 1fr씩 균등 분할. 계약/점검 일정 우측 여백 복원(노후도 분석 패널과 붙어버리던 현상 해소).
- **2026-06-02** — 대시보드 3열 균등 그리드 재구조화. `dash-cols(67%+33%)` 중첩 구조 → `dash-grid(repeat(3,1fr))` 플랫 구조로 교체. gap 14px(kpi-row와 동일) → KPI 6단과 패널 3단이 2칸씩 정확히 정렬. 우측 패널(지역별+노후도)은 col 3 rows 1-2 전체 점유. 계약/점검 일정은 DOM 유지·시각 숨김. 1080p에서도 kpi-row gap 12px 동기화.
- **2026-06-02** — 계약/점검 일정 패널 복원 + 하단 레이아웃 원본 디자인 일치. `dg-bot` 래퍼(grid-column:1/3, row 2)를 추가해 하단 3개 패널(TOP5·이슈·일정)이 6단 중 4단 영역을 3등분. 우측 패널은 col 3 rows 1-2 유지. 원본 디자인과 동일하게 상단 패널(2단씩)과 하단 패널(4/3단씩)이 정렬됨.
- **2026-06-02** — Vercel 배포 완료. Vercel CLI 설치 후 `vercel --prod`로 첫 배포. 접속 주소: https://atg-asset.vercel.app. GitHub 저장소와 자동 연결되어 이후 `git push` 시 자동 배포됨.
- **2026-06-02** — 최근 이슈 내역 2건(제주 UPS 경보·전국 PC 보안 패치) + 계약/점검 일정 2건(복합기 계약 갱신·소방 설비 점검) 추가. 항목 세로 여백 13→9px 줄여 5건이 스크롤 없이 표시되도록 최적화.
- **2026-06-02** — Supabase 연동 1단계 완료. `supabase/schema.sql`(테이블 생성), `supabase/seed.html`(샘플 71건 삽입 도구), `js/db.js`(Supabase 클라이언트·로드·저장 함수) 추가. `app.js` IIFE를 async로 변환해 Supabase 데이터 우선 로드, 실패 시 로컬 데이터로 자동 폴백. 자산 등록 모달이 화면+DB 동시 저장. DB 로딩 중 스피너 오버레이 표시.
